name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-favorites:
    name: Tests service-favorites
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: service-favorites/package-lock.json

      - name: Instal·lar dependències
        run: npm install
        working-directory: service-favorites

      - name: Verificar sintaxi (build)
        run: npm run build
        working-directory: service-favorites

      - name: Comprovar qualitat del codi (lint)
        run: npm run lint
        working-directory: service-favorites

      - name: Executar tests
        run: npm test
        working-directory: service-favorites

      - name: Notificar fallida
        if: failure()
        run: echo "❌ El workflow ha fallat a service-favorites. Revisa els errors anteriors."

  test-wishlist:
    name: Tests service-wishlist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: service-wishlist/package-lock.json

      - name: Instal·lar dependències
        run: npm install
        working-directory: service-wishlist

      - name: Verificar sintaxi (build)
        run: npm run build
        working-directory: service-wishlist

      - name: Comprovar qualitat del codi (lint)
        run: npm run lint
        working-directory: service-wishlist

      - name: Executar tests
        run: npm test
        working-directory: service-wishlist

      - name: Notificar fallida
        if: failure()
        run: echo "❌ El workflow ha fallat a service-wishlist. Revisa els errors anteriors."

  test-history:
    name: Tests service-history
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: service-history/package-lock.json

      - name: Instal·lar dependències
        run: npm install
        working-directory: service-history

      - name: Verificar sintaxi (build)
        run: npm run build
        working-directory: service-history

      - name: Comprovar qualitat del codi (lint)
        run: npm run lint
        working-directory: service-history

      - name: Executar tests
        run: npm test
        working-directory: service-history

      - name: Notificar fallida
        if: failure()
        run: echo "❌ El workflow ha fallat a service-history. Revisa els errors anteriors."
